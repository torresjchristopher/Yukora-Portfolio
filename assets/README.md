# Assets

Shared website assets are grouped by responsibility:

- `brand/` — favicons, marks, and identity files used across Yukora surfaces
- `site/` — future homepage-specific photography and media
- existing files and folders — legacy assets retained for route compatibility

Place new files in the appropriate subfolder rather than at the repository root.